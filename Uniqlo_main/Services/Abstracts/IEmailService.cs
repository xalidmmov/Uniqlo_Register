namespace Uniqlo_main.Services.Abstracts
{
    public interface IEmailService
    {
        //void SendEmailConfirmation(string email, string userName, string token);
        //void SendEmailConfirmationAsync(string receiver,string name,string token);
    }
}
